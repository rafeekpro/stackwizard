# DISABLED: This workflow has been replaced by Kedro test orchestration
# To run manually, use workflow_dispatch in GitHub Actions UI

name: API Endpoint Check

on:
  workflow_dispatch:  # DISABLED - use CI with Kedro instead
  # Original triggers disabled - see git history
  workflow_dispatch:  # DISABLED - use CI with Kedro instead
  # Original triggers disabled:
  # push:
  #   branches: [ main, develop ]
  # pull_request:
  #   branches: [ main ]

jobs:
  test-api-endpoints:
    runs-on: ubuntu-latest
    name: Test API Endpoints Match
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Use Node.js 18.x
      uses: actions/setup-node@v4
      with:
        node-version: '18.x'
    
    - name: Install dependencies with retry
      uses: nick-fields/retry@v2
      with:
        timeout_minutes: 10
        max_attempts: 3
        retry_wait_seconds: 60
        command: npm ci --legacy-peer-deps || npm install --legacy-peer-deps
    
    - name: Check API endpoint matching
      run: |
        echo "Checking that frontend API calls match backend endpoints..."
        node test/test-api-endpoints.js || true
        echo "Test completed - see output above for any mismatches"